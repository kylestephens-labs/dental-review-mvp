#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running prove:quick before push..."
npm run prove:quick

if [ $? -ne 0 ]; then
  echo "❌ prove:quick failed - push blocked"
  echo "💡 Fix the issues above and try again"
  exit 1
fi

echo "✅ prove:quick passed - push allowed"
